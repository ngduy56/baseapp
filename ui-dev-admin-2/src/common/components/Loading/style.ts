import { makeStyles } from '@material-ui/core';
export const useStyles = makeStyles({
  loading: {
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  loading__gif: {
    width: '300px',
    display: 'block',
  },
});
